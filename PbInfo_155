#include <bits/stdc++.h>

using namespace std;
int n,a,maxi;
bool ok;
char v[256],c,maxim;
int main()
{
    cin.get(v,256);
    n=strlen(v);
    for(int i=0;i<n;i++)
    {
        if(isdigit(v[i])){
        a=0;
        for(int j=i;j<n;j++){
            if(v[i]==v[j]){
                a++;
            }
        }
        if(maxi<a){
            maxim=v[i];
        }
        }
    }
cout<<maxim;
}
